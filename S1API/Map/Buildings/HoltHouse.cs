namespace S1API.Map.Buildings
{
	/// <summary>
	/// Identifier for the base-game building named "Holt House".
	/// Modders can use <see cref="Building.Get{HoltHouse}()"/> to resolve it.
	/// </summary>
	[BuildingName("Holt House")]
	public sealed class HoltHouse : IBuildingIdentifier { }
}
